Object name = "junayed";
var newname = "juna";
String name1 = "Junayed Ahamed";

void main() {
  print(name);
  print(newname);
  print(name1);

  // var i = -4.22;
  // print(i.abs());

  // int? value;
  // assert(value == null);

  //if initialization is null safe then we can take null and also some value
  //but if initialization is non nullable then then value should have to an exact value it can't be null
}
