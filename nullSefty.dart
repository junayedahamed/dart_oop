convert_string_into_int(value) {
  print(int.parse(value));
}

void main() {
  String?
      name; //we use (?) for saying a vale is null during time of initialization
  print(name);
  convert_string_into_int('45');
}
